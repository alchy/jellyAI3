# V2a — syntetická QA pipeline (qagen) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Poznámka k realizaci (preference uživatele):** většinu kódu píše Claude sám; subagentům jen jednoznačně triviální kroky, zdůvodněné.

**Goal:** Z korpusu vyrobit dataset dvojic otázka→odpověď (JSONL) pomocí ÚFAL NameTag+MorphoDiTa, jako tréninková data pro generátor V2b.

**Architecture:** Balíček `qagen/`: `Tagger` rozhraní (izoluje ÚFAL) → výběr odpovědi + typ (`answers`) → šablona otázky (`questions`) → sestavení datasetu (`build`). Hermetické testy přes `FakeTagger`; reálný `UfalTagger` + stažení modelů zvlášť, integrační test skipif.

**Tech Stack:** Python 3.11, `ufal.morphodita`, `ufal.nametag` (+ modely z LINDAT), stdlib (json, re, urllib). Znovu použije V1 bloky `loader`, `chunker`, `text`.

## Global Constraints

- Python **3.11** ve venv.
- **Hermetické testy** — nepoužívají síť ani stažené modely; ÚFAL je za rozhraním `Tagger` a testy jedou přes `FakeTagger`. Test reálného `UfalTagger` je `@pytest.mark.skipif`, když modely nejsou.
- Nové závislosti pouze: `ufal.morphodita`, `ufal.nametag`. Modely CC BY-NC-SA (nekomerční, pro osobní OK), lokálně/offline.
- **Zachovat plnou češtinu** (UTF-8, diakritika).
- Entitní typy dle CNEC: první písmeno typu — `p`=osoba, `g`=místo, `i`=instituce, `t`=čas.
- `data/qa/`, `data/models/` **gitignorovat**.
- Pracovní větev: `feature/v2a-qagen`.
- **Každá commit zpráva končí trailerem:**
  ```
  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
  ```

---

### Task 1: QagenConfig + gitignore

**Files:**
- Modify: `config.py`
- Modify: `.gitignore`
- Create: `qagen/__init__.py` (prázdný)
- Test: `tests/test_config.py` (přidat případ)

**Interfaces:**
- Produces: `QagenConfig` s poli `qa_path`, `morphodita_model`, `nametag_model`, `min_tokens`, `max_answers_per_sentence`, `types`; přidané do `Config` jako `qagen`.

- [ ] **Step 1: Přidat failing test**

Do `tests/test_config.py` přidej:
```python
def test_config_has_qagen_defaults():
    from config import Config
    cfg = Config()
    assert cfg.qagen.qa_path == "data/qa/qapairs.jsonl"
    assert cfg.qagen.min_tokens == 5
    assert cfg.qagen.max_answers_per_sentence == 2
    assert "Kdo" in cfg.qagen.types
```

- [ ] **Step 2: Spustit — musí selhat**

Run: `python -m pytest tests/test_config.py::test_config_has_qagen_defaults -v`
Expected: FAIL (`AttributeError: 'Config' object has no attribute 'qagen'`)

- [ ] **Step 3: Přidat QagenConfig do config.py**

Do `config.py` přidej třídu a pole do `Config`:
```python
@dataclass
class QagenConfig:
    """Nastavení generování syntetických QA párů.

    Atributy:
        qa_path (str): Kam zapsat JSONL dataset dvojic otázka→odpověď.
        morphodita_model (str): Cesta k modelu MorphoDiTa (POS/lemma).
        nametag_model (str): Cesta k modelu NameTag (NER).
        min_tokens (int): Minimální počet slov ve větě, aby měla smysl jako kontext.
        max_answers_per_sentence (int): Kolik nejvíc odpovědí vytěžit z jedné věty.
        types (tuple): Povolené typy otázek.
    """
    qa_path: str = "data/qa/qapairs.jsonl"
    morphodita_model: str = "data/models/czech-morfflex.tagger"
    nametag_model: str = "data/models/czech-cnec.ner"
    min_tokens: int = 5
    max_answers_per_sentence: int = 2
    types: tuple = ("Kdo", "Co", "Kde", "Kdy", "Kolik")
```
A do `Config` přidej pole (za `answerer`):
```python
    qagen: QagenConfig = field(default_factory=QagenConfig)
```

- [ ] **Step 4: Doplnit .gitignore a __init__.py**

Do `.gitignore` přidej:
```
data/qa/
data/models/
```
Vytvoř prázdný `qagen/__init__.py`.

- [ ] **Step 5: Spustit — musí projít**

Run: `python -m pytest tests/test_config.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add config.py .gitignore qagen/ tests/test_config.py
git commit -m "feat: QagenConfig + gitignore pro qa/modely"
```

---

### Task 2: Tagger rozhraní + datové typy + FakeTagger

**Files:**
- Create: `qagen/tagger.py`
- Test: `tests/test_qagen_tagger.py`

**Interfaces:**
- Consumes: nic.
- Produces:
  - dataclass `Entity(text: str, type: str, start: int, end: int)` — `start/end` = znakové offsety ve větě.
  - dataclass `Token(text: str, lemma: str, pos: str, start: int, end: int)` — `pos` = první písmeno PDT tagu.
  - `Tagger` Protocol s `entities(text) -> list[Entity]` a `tokens(text) -> list[Token]`.
  - `FakeTagger(entities=None, tokens=None)` — vrací nakonzervovaná data podle přesného textu; `[]` pro neznámé.

- [ ] **Step 1: Přidat failing test**

`tests/test_qagen_tagger.py`:
```python
from qagen.tagger import Entity, Token, FakeTagger


def test_fake_tagger_returns_canned():
    s1 = "Roboty vynalezl starý Rossum."
    s2 = "Sluneční soustava má osm planet."
    ft = FakeTagger(
        entities={s1: [Entity("starý Rossum", "P", 16, 28)]},
        tokens={s2: [Token("osm", "osm", "C", 21, 24)]},
    )
    assert ft.entities(s1)[0].text == "starý Rossum"
    assert ft.entities(s1)[0].type == "P"
    assert ft.entities("neznámá věta") == []
    assert ft.tokens(s2)[0].pos == "C"
    assert ft.tokens("neznámá věta") == []
```

- [ ] **Step 2: Spustit — musí selhat**

Run: `python -m pytest tests/test_qagen_tagger.py -v`
Expected: FAIL (`ModuleNotFoundError: No module named 'qagen.tagger'`)

- [ ] **Step 3: Napsat qagen/tagger.py (bez UfalTagger — ten přijde v Tasku 6)**

`qagen/tagger.py`:
```python
"""Rozhraní pro morfologicko-entitní analýzu češtiny.

Celá QA pipeline potřebuje vědět dvě věci o větě: kde jsou pojmenované entity
(kdo/co/kde/kdy — kandidáti na odpovědi) a jaké mají slova slovní druh a lemma.
Tuto znalost dodává ÚFAL (NameTag + MorphoDiTa), ale schováváme ji za jednoduché
rozhraní `Tagger`. Díky tomu zbytek pipeline neví nic o konkrétní knihovně a jde
testovat s `FakeTagger` — bez stahování stovek MB modelů jen kvůli testům.
"""

from dataclasses import dataclass
from typing import Protocol


@dataclass
class Entity:
    """Pojmenovaná entita nalezená ve větě (kandidát na odpověď).

    Atributy:
        text (str): Doslovný text entity.
        type (str): CNEC typ (první písmeno rozhoduje: p=osoba, g=místo,
            i=instituce, t=čas).
        start (int): Znakový offset začátku ve větě.
        end (int): Znakový offset za koncem (exkluzivní).
    """
    text: str
    type: str
    start: int
    end: int


@dataclass
class Token:
    """Jedno slovo s morfologickou anotací.

    Atributy:
        text (str): Tvar slova ve větě.
        lemma (str): Základní tvar (lemma).
        pos (str): Slovní druh — první písmeno PDT tagu (N=podst., C=číslovka…).
        start (int): Znakový offset začátku ve větě.
        end (int): Znakový offset za koncem (exkluzivní).
    """
    text: str
    lemma: str
    pos: str
    start: int
    end: int


class Tagger(Protocol):
    """Kontrakt, který qagen potřebuje: entity a otagované tokeny věty."""

    def entities(self, text: str) -> list:
        """Vrátí pojmenované entity ve větě (list[Entity])."""
        ...

    def tokens(self, text: str) -> list:
        """Vrátí otagované tokeny věty (list[Token])."""
        ...


class FakeTagger:
    """Testovací tagger — vrací předem připravená data podle přesného textu.

    Umožňuje testovat celou pipeline deterministicky a offline, bez ÚFAL modelů.

    Args:
        entities (dict|None): mapa text věty → list[Entity].
        tokens (dict|None): mapa text věty → list[Token].
    """

    def __init__(self, entities=None, tokens=None):
        self._entities = entities or {}
        self._tokens = tokens or {}

    def entities(self, text):
        return self._entities.get(text, [])

    def tokens(self, text):
        return self._tokens.get(text, [])
```

- [ ] **Step 4: Spustit — musí projít**

Run: `python -m pytest tests/test_qagen_tagger.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add qagen/tagger.py tests/test_qagen_tagger.py
git commit -m "feat: Tagger rozhraní + datové typy + FakeTagger"
```

---

### Task 3: Výběr odpovědí a typu otázky (answers.py)

**Files:**
- Create: `qagen/answers.py`
- Test: `tests/test_qagen_answers.py`

**Interfaces:**
- Consumes: `Entity`/`Token` (Task 2), `QagenConfig` (Task 1).
- Produces:
  - dataclass `Candidate(answer: str, qtype: str, start: int, end: int)`.
  - `candidates(sentence: str, tagger, config: QagenConfig) -> list[Candidate]`.

- [ ] **Step 1: Přidat failing test**

`tests/test_qagen_answers.py`:
```python
from config import QagenConfig
from qagen.tagger import Entity, Token, FakeTagger
from qagen.answers import candidates, Candidate


def test_candidate_from_person_entity():
    s = "Roboty vynalezl starý Rossum."
    ft = FakeTagger(entities={s: [Entity("starý Rossum", "P", 16, 28)]})
    cands = candidates(s, ft, QagenConfig())
    assert isinstance(cands[0], Candidate)
    assert cands[0].qtype == "Kdo"
    assert cands[0].answer == "starý Rossum"


def test_candidate_from_number_token():
    s = "Sluneční soustava má osm planet."
    ft = FakeTagger(tokens={s: [Token("osm", "osm", "C", 21, 24)]})
    cands = candidates(s, ft, QagenConfig())
    assert any(c.qtype == "Kolik" and c.answer == "osm" for c in cands)


def test_answer_equal_to_whole_sentence_skipped():
    s = "Rossum"
    ft = FakeTagger(entities={s: [Entity("Rossum", "P", 0, 6)]})
    assert candidates(s, ft, QagenConfig()) == []


def test_respects_max_answers():
    s = "A B C"
    ft = FakeTagger(entities={s: [
        Entity("A", "P", 0, 1), Entity("B", "P", 2, 3), Entity("C", "P", 4, 5),
    ]})
    cfg = QagenConfig(max_answers_per_sentence=2)
    assert len(candidates(s, ft, cfg)) == 2
```

- [ ] **Step 2: Spustit — musí selhat**

Run: `python -m pytest tests/test_qagen_answers.py -v`
Expected: FAIL (`ModuleNotFoundError`)

- [ ] **Step 3: Napsat qagen/answers.py**

`qagen/answers.py`:
```python
"""Výběr kandidátních odpovědí z věty a přiřazení typu otázky.

Odpověď na faktickou otázku bývá pojmenovaná entita (osoba, místo, instituce,
čas) nebo číslo. Tenhle modul takové spány z věty vytáhne a rovnou k nim přiřadí
tázací slovo — z „starý Rossum" (osoba) se stane odpověď na otázku typu „Kdo".
"""

from dataclasses import dataclass

# První písmeno CNEC typu → tázací slovo.
_ENTITY_QTYPE = {"p": "Kdo", "g": "Kde", "i": "Co", "t": "Kdy"}


@dataclass
class Candidate:
    """Kandidát na (odpověď, typ otázky) v rámci jedné věty.

    Atributy:
        answer (str): Text odpovědi (spán ve větě).
        qtype (str): Typ otázky (Kdo/Co/Kde/Kdy/Kolik).
        start (int): Znakový offset odpovědi ve větě.
        end (int): Znakový offset za koncem (exkluzivní).
    """
    answer: str
    qtype: str
    start: int
    end: int


def candidates(sentence, tagger, config):
    """Najde ve větě kandidátní odpovědi a jejich typy otázek.

    Z pojmenovaných entit (NameTag) odvodí Kdo/Co/Kde/Kdy, z číslovek
    (MorphoDiTa POS „C") odvodí Kolik. Vynechá odpovědi rovné celé větě
    (neměly by kontext), deduplikuje a omezí počet na `max_answers_per_sentence`.

    Args:
        sentence (str): Věta k analýze.
        tagger (Tagger): Zdroj entit a tokenů.
        config (QagenConfig): Nastavení (povolené typy, max odpovědí).

    Returns:
        list[Candidate]: Kandidáti v pořadí entity → čísla.
    """
    raw = []
    for e in tagger.entities(sentence):
        qt = _ENTITY_QTYPE.get(e.type[:1].lower())
        if qt:
            raw.append(Candidate(e.text, qt, e.start, e.end))
    for t in tagger.tokens(sentence):
        if t.pos == "C":
            raw.append(Candidate(t.text, "Kolik", t.start, t.end))

    result = []
    seen = set()
    stripped = sentence.strip()
    for c in raw:
        if c.qtype not in config.types:
            continue
        if c.answer.strip() == stripped:      # odpověď = celá věta → přeskoč
            continue
        key = (c.answer, c.start)
        if key in seen:
            continue
        seen.add(key)
        result.append(c)
        if len(result) >= config.max_answers_per_sentence:
            break
    return result
```

- [ ] **Step 4: Spustit — musí projít**

Run: `python -m pytest tests/test_qagen_answers.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add qagen/answers.py tests/test_qagen_answers.py
git commit -m "feat: výběr odpovědí a typu otázky z entit/čísel"
```

---

### Task 4: Šablona otázky (questions.py)

**Files:**
- Create: `qagen/questions.py`
- Test: `tests/test_qagen_questions.py`

**Interfaces:**
- Consumes: `Candidate` (Task 3).
- Produces: `build_question(sentence: str, candidate: Candidate) -> str`.

- [ ] **Step 1: Přidat failing test**

`tests/test_qagen_questions.py`:
```python
from qagen.answers import Candidate
from qagen.questions import build_question


def test_build_question_person():
    s = "Roboty vynalezl starý Rossum."
    c = Candidate(answer="starý Rossum", qtype="Kdo", start=16, end=28)
    assert build_question(s, c) == "Kdo roboty vynalezl?"


def test_build_question_number():
    s = "Sluneční soustava má osm planet."
    c = Candidate(answer="osm", qtype="Kolik", start=21, end=24)
    assert build_question(s, c) == "Kolik sluneční soustava má planet?"
```

- [ ] **Step 2: Spustit — musí selhat**

Run: `python -m pytest tests/test_qagen_questions.py -v`
Expected: FAIL (`ModuleNotFoundError`)

- [ ] **Step 3: Napsat qagen/questions.py**

`qagen/questions.py`:
```python
"""Sestavení otázky z věty a vybrané odpovědi.

Trik je jednoduchý a využívá volného slovosledu češtiny: z věty vyřízneme
odpovědní spán, zbytek očistíme a předsadíme tázací slovo. Z „Roboty vynalezl
starý Rossum." (odpověď „starý Rossum") tak vznikne „Kdo roboty vynalezl?".
Není to vždy stylisticky dokonalé, ale gramaticky to drží a hlavně je pár
(otázka, kontext, odpověď) koherentní — přesně to, z čeho se má model učit.
"""

import re


def build_question(sentence, candidate):
    """Složí otázku odstraněním odpovědi z věty a předsazením tázacího slova.

    Args:
        sentence (str): Původní věta.
        candidate (Candidate): Odpověď (se znakovými offsety) a typ otázky.

    Returns:
        str: Otázka ve tvaru „<tázací slovo> <zbytek věty>?".
    """
    remainder = sentence[:candidate.start] + " " + sentence[candidate.end:]
    remainder = re.sub(r"\s+", " ", remainder).strip()
    remainder = remainder.rstrip(" .!?,;:").strip()
    if remainder:
        remainder = remainder[0].lower() + remainder[1:]
    question = f"{candidate.qtype} {remainder}?"
    return re.sub(r"\s+", " ", question).strip()
```

- [ ] **Step 4: Spustit — musí projít**

Run: `python -m pytest tests/test_qagen_questions.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add qagen/questions.py tests/test_qagen_questions.py
git commit -m "feat: šablona otázky (odstranění odpovědi + tázací slovo)"
```

---

### Task 5: Sestavení datasetu (build.py)

**Files:**
- Create: `qagen/build.py`
- Test: `tests/test_qagen_build.py`

**Interfaces:**
- Consumes: `load_documents` (V1), `chunk` (V1), `split_sentences` (V1), `candidates` (Task 3), `build_question` (Task 4), `Config`.
- Produces: `build_dataset(config: Config, tagger) -> list[dict]` — zapíše JSONL na `config.qagen.qa_path` a vrátí seznam párů.

- [ ] **Step 1: Přidat failing test**

`tests/test_qagen_build.py`:
```python
import json

from config import Config, DataConfig, QagenConfig
from qagen.tagger import Entity, FakeTagger
from qagen.build import build_dataset


def test_build_dataset(tmp_path):
    proc = tmp_path / "processed"
    proc.mkdir()
    s = "Roboty vynalezl starý Rossum."
    (proc / "rur.txt").write_text(s + " Helena přijela na ostrov.", encoding="utf-8")
    qa = tmp_path / "qa" / "qapairs.jsonl"
    cfg = Config(
        data=DataConfig(processed_dir=str(proc)),
        qagen=QagenConfig(qa_path=str(qa), min_tokens=3),
    )
    ft = FakeTagger(entities={s: [Entity("starý Rossum", "P", 16, 28)]})
    pairs = build_dataset(cfg, ft)

    assert len(pairs) == 1
    assert pairs[0]["question"] == "Kdo roboty vynalezl?"
    assert pairs[0]["answer"] == "starý Rossum"
    assert pairs[0]["type"] == "Kdo"
    assert pairs[0]["doc_id"] == "rur"
    assert qa.exists()
    first = json.loads(qa.read_text(encoding="utf-8").splitlines()[0])
    assert first["question"] == "Kdo roboty vynalezl?"
```

- [ ] **Step 2: Spustit — musí selhat**

Run: `python -m pytest tests/test_qagen_build.py -v`
Expected: FAIL (`ModuleNotFoundError`)

- [ ] **Step 3: Napsat qagen/build.py**

`qagen/build.py`:
```python
"""Sestavení celého QA datasetu z korpusu.

Spojí V1 bloky (loader → chunker → věty) s qagen logikou (výběr odpovědí →
šablona otázky) a zapíše výsledek jako JSONL — jeden pár na řádek. Kontextem
každého páru je celá pasáž, aby měl budoucí generátor z čeho čerpat. Duplicity
se odfiltrují, ať dataset není zaplavený týmiž otázkami.
"""

import json
import os

from jellyai.loader import load_documents
from jellyai.chunker import chunk
from jellyai.text import split_sentences
from qagen.answers import candidates
from qagen.questions import build_question


def build_dataset(config, tagger):
    """Vygeneruje QA páry z korpusu a zapíše je do JSONL.

    Args:
        config (Config): Konfigurace (processed_dir, chunker, qagen).
        tagger (Tagger): Analyzátor entit/tokenů (UfalTagger nebo FakeTagger).

    Returns:
        list[dict]: Páry {question, context, answer, type, doc_id, passage_index}.
    """
    docs = load_documents(config.data.processed_dir)
    directory = os.path.dirname(config.qagen.qa_path)
    if directory:
        os.makedirs(directory, exist_ok=True)

    pairs = []
    seen = set()
    for doc in docs:
        for passage in chunk(doc, config.chunker):
            for sentence in split_sentences(passage.text):
                if len(sentence.split()) < config.qagen.min_tokens:
                    continue
                for cand in candidates(sentence, tagger, config.qagen):
                    question = build_question(sentence, cand)
                    key = (question, cand.answer, doc.doc_id)
                    if key in seen:
                        continue
                    seen.add(key)
                    pairs.append({
                        "question": question,
                        "context": passage.text,
                        "answer": cand.answer,
                        "type": cand.qtype,
                        "doc_id": doc.doc_id,
                        "passage_index": passage.index,
                    })

    with open(config.qagen.qa_path, "w", encoding="utf-8") as f:
        for pair in pairs:
            f.write(json.dumps(pair, ensure_ascii=False) + "\n")
    return pairs
```

- [ ] **Step 4: Spustit — musí projít**

Run: `python -m pytest tests/test_qagen_build.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add qagen/build.py tests/test_qagen_build.py
git commit -m "feat: sestavení QA datasetu (JSONL) z korpusu"
```

---

### Task 6: Reálný UfalTagger + stažení modelů

**Files:**
- Modify: `qagen/tagger.py` (přidat `UfalTagger`)
- Create: `qagen/download_models.py`
- Create: `requirements-v2.txt`
- Test: `tests/test_qagen_ufal.py` (integrační, `skipif`)

**Interfaces:**
- Consumes: `Entity`/`Token` (Task 2), `QagenConfig`.
- Produces:
  - `UfalTagger(morphodita_model: str, nametag_model: str)` s `entities`/`tokens` jako `FakeTagger`.
  - `download_models(config) -> list[tuple[str, bool]]`.

**Pozn. k realizaci:** přesné volání bindingů `ufal.morphodita`/`ufal.nametag` a URL modelů z LINDAT se ověří při implementaci s nainstalovanými balíčky a staženými modely (hermetické testy Tasků 1–5 na tom nezávisí; tento integrační test je `skipif`).

- [ ] **Step 1: Napsat requirements-v2.txt**

`requirements-v2.txt`:
```
ufal.morphodita>=1.11
ufal.nametag>=1.2
```
Instalace: `pip install -r requirements-v2.txt`

- [ ] **Step 2: Přidat UfalTagger do qagen/tagger.py**

Na konec `qagen/tagger.py` přidej (importy ÚFAL jsou uvnitř metod/`__init__`, aby import modulu nevyžadoval nainstalované ÚFAL):
```python
def _pdt_pos(tag):
    """Vrátí slovní druh = první znak PDT tagu (N/A/C/V…)."""
    return tag[:1] if tag else ""


class UfalTagger:
    """Reálný tagger nad ÚFAL MorphoDiTa (POS/lemma) a NameTag (NER).

    Modely se načtou při vytvoření. Analýza probíhá po jedné větě, offsety jsou
    znakové v rámci předané věty (shodně s FakeTagger).

    Args:
        morphodita_model (str): Cesta k modelu MorphoDiTa.
        nametag_model (str): Cesta k modelu NameTag.
    """

    def __init__(self, morphodita_model, nametag_model):
        from ufal.morphodita import Tagger as MorphoTagger
        from ufal.nametag import Ner
        self._morpho = MorphoTagger.load(morphodita_model)
        if self._morpho is None:
            raise FileNotFoundError(f"Nelze načíst MorphoDiTa model: {morphodita_model}")
        self._ner = Ner.load(nametag_model)
        if self._ner is None:
            raise FileNotFoundError(f"Nelze načíst NameTag model: {nametag_model}")

    def tokens(self, text):
        from ufal.morphodita import Forms, TaggedLemmas, TokenRanges
        forms, lemmas, ranges = Forms(), TaggedLemmas(), TokenRanges()
        tokenizer = self._morpho.newTokenizer()
        tokenizer.setText(text)
        out = []
        while tokenizer.nextSentence(forms, ranges):
            self._morpho.tag(forms, lemmas)
            for i in range(len(lemmas)):
                start = ranges[i].start
                length = ranges[i].length
                out.append(Token(
                    text=text[start:start + length],
                    lemma=lemmas[i].lemma,
                    pos=_pdt_pos(lemmas[i].tag),
                    start=start,
                    end=start + length,
                ))
        return out

    def entities(self, text):
        from ufal.nametag import Forms, TokenRanges, NamedEntities
        forms, ranges, ents = Forms(), TokenRanges(), NamedEntities()
        tokenizer = self._ner.newTokenizer()
        tokenizer.setText(text)
        out = []
        while tokenizer.nextSentence(forms, ranges):
            self._ner.recognize(forms, ents)
            for ent in ents:
                first = ent.start
                last = ent.start + ent.length - 1
                start = ranges[first].start
                end = ranges[last].start + ranges[last].length
                out.append(Entity(
                    text=text[start:end],
                    type=ent.type,
                    start=start,
                    end=end,
                ))
        return out
```

- [ ] **Step 3: Napsat qagen/download_models.py**

`qagen/download_models.py`:
```python
"""Stažení českých modelů pro MorphoDiTa a NameTag z LINDAT.

Modely jsou CC BY-NC-SA (nekomerční) — pro osobní/výukový projekt v pořádku.
Stahování je best-effort: když zdroj selže, ohlásí to a pokračuje. Přesné URL
se ověří při implementaci (LINDAT repozitář ÚFAL).
"""

import os
import urllib.request

# (url, cílová cesta) — ověřeno při implementaci proti aktuálním LINDAT balíčkům.
MODELS = [
    # ("https://lindat.mff.cuni.cz/.../czech-morfflex2.0-pdtc1.0.tagger",
    #  "data/models/czech-morfflex.tagger"),
    # ("https://lindat.mff.cuni.cz/.../czech-cnec2.0.ner",
    #  "data/models/czech-cnec.ner"),
]


def download_models(config=None, models=MODELS):
    """Stáhne modely do data/models. Vrátí (cesta, úspěch) pro každý.

    Args:
        config: Nepovinné (rezervováno pro budoucí cesty z konfigurace).
        models (list): Dvojice (url, cílová cesta).

    Returns:
        list[tuple[str, bool]]: Výsledek stažení pro každý model.
    """
    results = []
    for url, dest in models:
        os.makedirs(os.path.dirname(dest), exist_ok=True)
        try:
            urllib.request.urlretrieve(url, dest)
            print(f"Staženo: {dest}")
            results.append((dest, True))
        except Exception as exc:  # noqa: BLE001
            print(f"Přeskočeno {url}: {exc}")
            results.append((dest, False))
    return results
```

- [ ] **Step 4: Napsat integrační test (skipif)**

`tests/test_qagen_ufal.py`:
```python
import os

import pytest

from config import QagenConfig
from qagen.answers import candidates

_cfg = QagenConfig()
_models_present = os.path.exists(_cfg.morphodita_model) and os.path.exists(_cfg.nametag_model)


@pytest.mark.skipif(not _models_present, reason="ÚFAL modely nejsou stažené")
def test_ufal_tagger_finds_person():
    from qagen.tagger import UfalTagger
    tagger = UfalTagger(_cfg.morphodita_model, _cfg.nametag_model)
    cands = candidates("Roboty vynalezl starý Rossum.", tagger, _cfg)
    assert any(c.qtype == "Kdo" for c in cands)
```

- [ ] **Step 5: Spustit — hermetická sada projde, integrační se přeskočí**

Run: `python -m pytest tests/test_qagen_ufal.py -v`
Expected: SKIPPED (modely nejsou) — a `python -m pytest -q` celkově PASS.

- [ ] **Step 6: Commit**

```bash
git add qagen/tagger.py qagen/download_models.py requirements-v2.txt tests/test_qagen_ufal.py
git commit -m "feat: UfalTagger (NameTag+MorphoDiTa) + stažení modelů"
```

---

### Task 7: CLI + wrapper + README

**Files:**
- Modify: `cli.py` (příkazy `gen-qa`, `qa-models`)
- Modify: `jelly` (subcommandy `qa`, `qa-models`)
- Modify: `README.md` (sekce V2a)
- Test: `tests/test_cli.py` (přidat `gen-qa` s injektovaným FakeTagger)

**Interfaces:**
- Consumes: `build_dataset` (Task 5), `download_models` (Task 6), `UfalTagger` (Task 6), `Config`.
- Produces: `cmd_gen_qa(config, tagger=None) -> int`, `cmd_qa_models(config) -> list`.

- [ ] **Step 1: Přidat failing test (gen-qa s FakeTagger)**

Do `tests/test_cli.py` přidej:
```python
def test_cmd_gen_qa_with_fake_tagger(tmp_path):
    from config import Config, DataConfig, QagenConfig
    from qagen.tagger import Entity, FakeTagger
    import cli

    proc = tmp_path / "processed"
    proc.mkdir()
    s = "Roboty vynalezl starý Rossum."
    (proc / "rur.txt").write_text(s, encoding="utf-8")
    cfg = Config(
        data=DataConfig(processed_dir=str(proc)),
        qagen=QagenConfig(qa_path=str(tmp_path / "qa" / "p.jsonl"), min_tokens=3),
    )
    ft = FakeTagger(entities={s: [Entity("starý Rossum", "P", 16, 28)]})
    n = cli.cmd_gen_qa(cfg, tagger=ft)
    assert n == 1
    assert (tmp_path / "qa" / "p.jsonl").exists()
```

- [ ] **Step 2: Spustit — musí selhat**

Run: `python -m pytest tests/test_cli.py::test_cmd_gen_qa_with_fake_tagger -v`
Expected: FAIL (`AttributeError: module 'cli' has no attribute 'cmd_gen_qa'`)

- [ ] **Step 3: Přidat příkazy do cli.py**

Do `cli.py` přidej importy a funkce:
```python
from qagen.build import build_dataset
from qagen.download_models import download_models


def cmd_qa_models(config):
    """Stáhne ÚFAL modely (MorphoDiTa + NameTag) pro generování QA dat.

    Args:
        config (Config): Konfigurace (rezervováno pro cesty modelů).

    Returns:
        list[tuple[str, bool]]: Výsledek stažení každého modelu.
    """
    return download_models(config)


def cmd_gen_qa(config, tagger=None):
    """Vygeneruje syntetický QA dataset z korpusu.

    Args:
        config (Config): Konfigurace (processed_dir, chunker, qagen).
        tagger (Tagger|None): Analyzátor; když None, vytvoří se UfalTagger z modelů
            v konfiguraci. Injektování usnadňuje testy (FakeTagger).

    Returns:
        int: Počet vygenerovaných párů.
    """
    if tagger is None:
        from qagen.tagger import UfalTagger
        tagger = UfalTagger(config.qagen.morphodita_model, config.qagen.nametag_model)
    pairs = build_dataset(config, tagger)
    print(f"Vygenerováno {len(pairs)} QA párů → {config.qagen.qa_path}")
    return len(pairs)
```
V `_build_parser` přidej subparsery:
```python
    sub.add_parser("qa-models", parents=[common], help="stáhne ÚFAL modely")
    sub.add_parser("gen-qa", parents=[common], help="vygeneruje QA dataset z korpusu")
```
V `main` do větvení příkazů přidej:
```python
    elif args.command == "qa-models":
        cmd_qa_models(config)
    elif args.command == "gen-qa":
        cmd_gen_qa(config)
```

- [ ] **Step 4: Spustit — musí projít**

Run: `python -m pytest tests/test_cli.py -v`
Expected: PASS

- [ ] **Step 5: Přidat subcommandy do wrapperu `jelly`**

V `jelly` do seznamu povolených příkazů přidej `qa` a `qa-models` a do vnitřního `case`:
```bash
      qa-models) "$PY" "$ROOT/cli.py" qa-models ;;
      qa)        "$PY" "$ROOT/cli.py" gen-qa ;;
```
(A do řádku `prepare|index|ask|explain)` v horním `case` doplň `|qa|qa-models`.)

- [ ] **Step 6: Doplnit README (sekce V2a)**

Do `README.md` za sekci „Roadmapa" (nebo před ni) přidej:
```markdown
## V2a — syntetická QA data (příprava pro generátor)

Generuje dvojice otázka→odpověď z korpusu pomocí českých NLP modelů ÚFAL
(NameTag + MorphoDiTa). Vyžaduje extra závislosti a modely (CC BY-NC-SA):

```bash
pip install -r requirements-v2.txt
./jelly qa-models      # stáhne české modely (MorphoDiTa + NameTag)
./jelly qa             # vygeneruje data/qa/qapairs.jsonl z data/processed
```

Dataset je vstup pro trénink generátoru (V2b).
```

- [ ] **Step 7: Spustit celou sadu**

Run: `python -m pytest -q`
Expected: PASS (hermetické testy V1 + V2a; integrační ÚFAL test SKIPPED)

- [ ] **Step 8: Commit**

```bash
git add cli.py jelly README.md tests/test_cli.py
git commit -m "feat: CLI/wrapper gen-qa + qa-models, README V2a"
```

---

## Self-Review (kontrola plánu proti specu)

**Spec coverage:** Tagger rozhraní + FakeTagger (T2), výběr odpovědí entity+čísla→typ (T3, spec 3.2), šablona otázky (T4, spec 3.3), sestavení JSONL datasetu (T5, spec 3.4), UfalTagger + stažení modelů (T6, spec 3.1/3.5), QagenConfig (T1, spec 4), CLI/wrapper (T7). Hermetické testy přes FakeTagger, integrační skipif (spec 7). Ošetření: věta bez entit → žádný pár (T5), odpověď = celá věta → skip (T3), chybějící model → FileNotFoundError (T6), dedup (T3/T5). ✔

**Placeholder scan:** Kód hermetických tasků je kompletní a konkrétní. Vědomé deferrals: (a) přesné URL modelů v `download_models.MODELS` (ověří se proti LINDAT při implementaci — jako u knih ve V1; hermetické testy na tom nezávisí), (b) přesná volání ÚFAL bindingů v UfalTagger (ověří se s nainstalovanou knihovnou; T6 integrační test je skipif). Obojí je izolované za rozhraním a nezastaví hermetické testy. V Tasku 2 Step 3 je záměrně označený překlep `//` v docstringu s pokynem ho nepsat.

**Type consistency:** `Entity`/`Token`/`Candidate` a jejich pole se používají konzistentně; `candidates()` vrací `list[Candidate]`, který `build_question` i `build_dataset` konzumují stejně; `FakeTagger` i `UfalTagger` sdílejí kontrakt `entities`/`tokens` vracející stejné typy; offsety jsou všude znakové v rámci věty. Formát JSONL řádku je stejný v T5 i testech. ✔
