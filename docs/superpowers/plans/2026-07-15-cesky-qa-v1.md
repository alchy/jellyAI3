# Český QA V1 (retrieval + extraktivní odpověď) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Poznámka k realizaci (preference uživatele):** většinu kódu píše Claude sám; subagentům se delegují jen jednoznačně triviální/mechanické kroky, a to explicitně zdůvodněné.

**Goal:** Postavit funkční, plně lokální QA nad českými texty (start: Čapkovo R.U.R.), které na dotaz najde relevantní pasáž a vrátí z ní extraktivní odpověď se zdrojem.

**Architecture:** Skládatelné bloky knihovny `jellyai`: Loader → Chunker → Retriever (TF-IDF/BM25 od nuly v numpy) → ExtractiveAnswerer → QAPipeline. Datová příprava v `dataprep/`. Vše ovládané přes `cli.py`. Bez externích služeb, bez stažených modelů.

**Tech Stack:** Python 3.11/3.12, numpy, pytest, standardní knihovna (urllib, re, dataclasses, argparse).

## Global Constraints

- Python **3.11 nebo 3.12** ve venv (novější verze mají problém s torch, který přijde ve V2).
- Třetí strany pouze: **numpy** (běh) a **pytest** (testy). Vše ostatní stdlib. Žádné `requests`, žádný huggingface/transformers, žádná Ollama/API.
- **Zachovat plnou češtinu** — diakritika, interpunkce, velikost písmen se nikdy nezahazují.
- Všechny soubory a texty **UTF-8**.
- Každý blok knihovny má modulovou funkci `explain() -> str` (výuková vrstva).
- Testy jsou **hermetické** — nepoužívají síť ani stažená data; korpusová data si vyrábějí přes `tmp_path` fixtury.
- Pracovní větev: `feature/cesky-gpt`.
- **Každá commit zpráva končí trailerem:**
  ```
  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
  ```
  (V krocích níže je u `git commit` uvedený jen předmět; trailer doplň vždy.)

---

### Task 1: Scaffold projektu + konfigurace

**Files:**
- Create: `requirements.txt`
- Create: `.gitignore`
- Create: `config.py`
- Create: `jellyai/__init__.py` (prázdný)
- Create: `jellyai/answerer/__init__.py` (prázdný)
- Create: `dataprep/__init__.py` (prázdný)
- Create: `tests/__init__.py` (prázdný)
- Create: `tests/test_config.py`

**Interfaces:**
- Produces: dataclasses `DataConfig`, `ChunkerConfig`, `RetrieverConfig`, `AnswererConfig`, `Config` s výchozími hodnotami; `Config()` vytvoří kompletní vnořenou konfiguraci.

- [ ] **Step 1: Napsat requirements.txt a .gitignore**

`requirements.txt`:
```
numpy>=1.26
pytest>=8.0
```

`.gitignore` (přidej na konec, pokud soubor existuje):
```
.venv/
__pycache__/
*.pyc
data/raw/
data/processed/
*.egg-info/
.pytest_cache/
```

- [ ] **Step 2: Napsat failing test konfigurace**

`tests/test_config.py`:
```python
from config import Config, ChunkerConfig, RetrieverConfig


def test_config_defaults():
    cfg = Config()
    assert cfg.chunker.size == 3
    assert cfg.chunker.overlap == 1
    assert cfg.chunker.unit == "sentences"
    assert cfg.retriever.method == "bm25"
    assert cfg.retriever.top_k == 5
    assert cfg.retriever.use_stopwords is True
    assert cfg.answerer.template is True
    assert cfg.data.processed_dir == "data/processed"


def test_config_override():
    cfg = Config(retriever=RetrieverConfig(method="tfidf", top_k=3))
    assert cfg.retriever.method == "tfidf"
    assert cfg.retriever.top_k == 3
    # ostatní bloky zůstávají výchozí
    assert cfg.chunker.size == 3
```

- [ ] **Step 3: Spustit test — musí selhat**

Run: `python -m pytest tests/test_config.py -v`
Expected: FAIL (`ModuleNotFoundError: No module named 'config'`)

- [ ] **Step 4: Napsat config.py**

`config.py`:
```python
from dataclasses import dataclass, field


@dataclass
class DataConfig:
    raw_dir: str = "data/raw"
    processed_dir: str = "data/processed"
    # seznam (url, filename) public-domain knih; ověřeno při plnění dat
    books: list = field(default_factory=list)


@dataclass
class ChunkerConfig:
    size: int = 3            # počet vět na pasáž
    overlap: int = 1         # počet vět sdílených mezi sousedními pasážemi
    unit: str = "sentences"  # "sentences" | "chars"


@dataclass
class RetrieverConfig:
    method: str = "bm25"     # "bm25" | "tfidf"
    top_k: int = 5
    k1: float = 1.5          # BM25 parametr
    b: float = 0.75          # BM25 parametr
    use_stopwords: bool = True


@dataclass
class AnswererConfig:
    max_sentences: int = 1
    template: bool = True


@dataclass
class Config:
    data: DataConfig = field(default_factory=DataConfig)
    chunker: ChunkerConfig = field(default_factory=ChunkerConfig)
    retriever: RetrieverConfig = field(default_factory=RetrieverConfig)
    answerer: AnswererConfig = field(default_factory=AnswererConfig)
```

- [ ] **Step 5: Vytvořit prázdné `__init__.py`**

Vytvoř prázdné soubory: `jellyai/__init__.py`, `jellyai/answerer/__init__.py`, `dataprep/__init__.py`, `tests/__init__.py`.

- [ ] **Step 6: Spustit test — musí projít**

Run: `python -m pytest tests/test_config.py -v`
Expected: PASS (2 passed)

- [ ] **Step 7: Commit**

```bash
git add requirements.txt .gitignore config.py jellyai/ dataprep/ tests/
git commit -m "feat: scaffold projektu a konfigurace bloků"
```

---

### Task 2: Textové utility (tokenizace, věty, stopslova)

**Files:**
- Create: `jellyai/text.py`
- Test: `tests/test_text.py`

**Interfaces:**
- Consumes: nic.
- Produces:
  - `tokenize(text: str, stopwords: set[str] | None = None) -> list[str]` — lowercased slovní tokeny (unicode `\w+`, diakritika zachována), volitelně bez stopslov.
  - `split_sentences(text: str) -> list[str]` — seznam vět bez prázdných.
  - `CZECH_STOPWORDS: set[str]` — malá sada českých stopslov.

- [ ] **Step 1: Napsat failing test**

`tests/test_text.py`:
```python
from jellyai.text import tokenize, split_sentences, CZECH_STOPWORDS


def test_tokenize_keeps_diacritics_lowercases():
    assert tokenize("Příliš žluťoučký kůň") == ["příliš", "žluťoučký", "kůň"]


def test_tokenize_removes_stopwords():
    toks = tokenize("kdo vyráběl roboty", CZECH_STOPWORDS)
    assert "kdo" not in toks
    assert "roboty" in toks


def test_split_sentences():
    text = "Roboty vyráběla firma. Starý Rossum je vynalezl! Kdo to ví?"
    assert split_sentences(text) == [
        "Roboty vyráběla firma.",
        "Starý Rossum je vynalezl!",
        "Kdo to ví?",
    ]


def test_split_sentences_empty():
    assert split_sentences("   ") == []
```

- [ ] **Step 2: Spustit test — musí selhat**

Run: `python -m pytest tests/test_text.py -v`
Expected: FAIL (`ModuleNotFoundError: No module named 'jellyai.text'`)

- [ ] **Step 3: Napsat jellyai/text.py**

`jellyai/text.py`:
```python
import re

_WORD_RE = re.compile(r"\w+", re.UNICODE)
_SENT_RE = re.compile(r"(?<=[.!?…])\s+")

# Malá sada českých stopslov (funkční slova, tázací zájmena, spojky, předložky).
CZECH_STOPWORDS = {
    "a", "aby", "aj", "ale", "ani", "ano", "az", "až", "bez", "bude", "budou",
    "by", "byl", "byla", "byli", "bylo", "být", "co", "což", "či", "do", "i",
    "je", "jeho", "jej", "její", "jejich", "jen", "ještě", "jak", "jako", "jsem",
    "jsi", "jsme", "jsou", "jste", "k", "kam", "kde", "kdo", "když", "ke", "která",
    "které", "který", "kteří", "ku", "ma", "má", "mají", "mé", "mezi", "mi", "mít",
    "mne", "mně", "můj", "my", "na", "nad", "nade", "nam", "nám", "napiš", "naš",
    "náš", "ne", "nebo", "něco", "něj", "není", "než", "ní", "nic", "o", "od",
    "on", "ona", "oni", "ono", "pak", "po", "pod", "podle", "pokud", "pouze",
    "proč", "pro", "před", "přes", "při", "s", "se", "si", "ta", "tak", "také",
    "tam", "te", "tě", "ten", "tedy", "to", "tom", "tomu", "tuto", "ty", "u", "už",
    "v", "vám", "vas", "vás", "ve", "více", "však", "všech", "vy", "z", "za",
    "ze", "že",
}


def tokenize(text, stopwords=None):
    """Rozdělí text na slovní tokeny (lowercase, diakritika zachována)."""
    toks = [t.lower() for t in _WORD_RE.findall(text)]
    if stopwords:
        toks = [t for t in toks if t not in stopwords]
    return toks


def split_sentences(text):
    """Rozdělí text na věty podle koncové interpunkce."""
    text = text.strip()
    if not text:
        return []
    parts = _SENT_RE.split(text)
    return [p.strip() for p in parts if p.strip()]
```

- [ ] **Step 4: Spustit test — musí projít**

Run: `python -m pytest tests/test_text.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: Commit**

```bash
git add jellyai/text.py tests/test_text.py
git commit -m "feat: textové utility — tokenizace, věty, stopslova"
```

---

### Task 3: Čištění textu (dataprep/clean.py)

**Files:**
- Create: `dataprep/clean.py`
- Test: `tests/test_clean.py`

**Interfaces:**
- Consumes: nic.
- Produces:
  - `clean_text(raw: str) -> str` — odstraní Gutenberg hlavičku/patičku, sjednotí konce řádků a bílé znaky; zachová diakritiku/interpunkci/velikost.
  - `build_processed(raw_dir: str, processed_dir: str) -> list[str]` — vyčistí všechny `.txt` z `raw_dir` do `processed_dir`, vrátí seznam zapsaných cest.

- [ ] **Step 1: Napsat failing test**

`tests/test_clean.py`:
```python
from dataprep.clean import clean_text, build_processed


def test_clean_keeps_diacritics_and_punctuation():
    raw = "Příliš   žluťoučký\r\nkůň, úpěl!  "
    out = clean_text(raw)
    assert "ž" in out and "ě" in out
    assert "," in out and "!" in out
    assert "  " not in out  # zdvojené mezery sjednoceny


def test_clean_strips_gutenberg_boilerplate():
    raw = (
        "*** START OF THE PROJECT GUTENBERG EBOOK RUR ***\n"
        "Skutečný text díla.\n"
        "*** END OF THE PROJECT GUTENBERG EBOOK RUR ***\n"
        "License blabla"
    )
    out = clean_text(raw)
    assert "Skutečný text díla." in out
    assert "START OF" not in out
    assert "License" not in out


def test_build_processed(tmp_path):
    raw_dir = tmp_path / "raw"
    proc_dir = tmp_path / "processed"
    raw_dir.mkdir()
    (raw_dir / "kniha.txt").write_text("Text   knihy.", encoding="utf-8")
    written = build_processed(str(raw_dir), str(proc_dir))
    assert len(written) == 1
    assert (proc_dir / "kniha.txt").read_text(encoding="utf-8") == "Text knihy."
```

- [ ] **Step 2: Spustit test — musí selhat**

Run: `python -m pytest tests/test_clean.py -v`
Expected: FAIL (`ModuleNotFoundError: No module named 'dataprep.clean'`)

- [ ] **Step 3: Napsat dataprep/clean.py**

`dataprep/clean.py`:
```python
import os
import re

_GUTENBERG_START = re.compile(r"\*\*\*\s*START OF.*?\*\*\*", re.IGNORECASE | re.DOTALL)
_GUTENBERG_END = re.compile(r"\*\*\*\s*END OF.*", re.IGNORECASE | re.DOTALL)


def clean_text(raw):
    """Vyčistí syrový text; zachová diakritiku, interpunkci i velikost písmen."""
    text = raw
    m = _GUTENBERG_START.search(text)
    if m:
        text = text[m.end():]
    m = _GUTENBERG_END.search(text)
    if m:
        text = text[:m.start()]
    text = text.replace("\r\n", "\n").replace("\r", "\n")
    text = re.sub(r"[ \t]+", " ", text)
    text = re.sub(r" *\n *", "\n", text)
    text = re.sub(r"\n{3,}", "\n\n", text)
    return text.strip()


def build_processed(raw_dir, processed_dir):
    """Vyčistí všechny .txt z raw_dir do processed_dir. Vrátí zapsané cesty."""
    os.makedirs(processed_dir, exist_ok=True)
    written = []
    for name in sorted(os.listdir(raw_dir)):
        if not name.endswith(".txt"):
            continue
        with open(os.path.join(raw_dir, name), encoding="utf-8") as f:
            cleaned = clean_text(f.read())
        dest = os.path.join(processed_dir, name)
        with open(dest, "w", encoding="utf-8") as f:
            f.write(cleaned)
        written.append(dest)
    return written
```

- [ ] **Step 4: Spustit test — musí projít**

Run: `python -m pytest tests/test_clean.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add dataprep/clean.py tests/test_clean.py
git commit -m "feat: čištění textu se zachováním češtiny"
```

---

### Task 4: Loader (jellyai/loader.py)

**Files:**
- Create: `jellyai/loader.py`
- Test: `tests/test_loader.py`

**Interfaces:**
- Consumes: nic.
- Produces:
  - dataclass `Document(doc_id: str, title: str, text: str)`.
  - `load_documents(directory: str) -> list[Document]` — načte `.txt` z adresáře (seřazené), `doc_id` = název bez přípony.
  - `explain() -> str`.

- [ ] **Step 1: Napsat failing test**

`tests/test_loader.py`:
```python
from jellyai.loader import load_documents, Document, explain


def test_load_documents(tmp_path):
    (tmp_path / "rur.txt").write_text("Text R.U.R.", encoding="utf-8")
    (tmp_path / "babicka.txt").write_text("Text Babičky", encoding="utf-8")
    (tmp_path / "ignore.md").write_text("neload", encoding="utf-8")
    docs = load_documents(str(tmp_path))
    assert [d.doc_id for d in docs] == ["babicka", "rur"]  # seřazené
    assert isinstance(docs[0], Document)
    assert docs[1].text == "Text R.U.R."


def test_explain_nonempty():
    assert isinstance(explain(), str) and explain().strip()
```

- [ ] **Step 2: Spustit test — musí selhat**

Run: `python -m pytest tests/test_loader.py -v`
Expected: FAIL (`ModuleNotFoundError`)

- [ ] **Step 3: Napsat jellyai/loader.py**

`jellyai/loader.py`:
```python
import os
from dataclasses import dataclass


@dataclass
class Document:
    doc_id: str
    title: str
    text: str


def load_documents(directory):
    """Načte všechny .txt z adresáře do Document objektů (seřazené podle jména)."""
    if not os.path.isdir(directory):
        raise FileNotFoundError(
            f"Adresář s texty neexistuje: {directory}. "
            f"Spusť nejdřív `python cli.py prepare-data`."
        )
    docs = []
    for name in sorted(os.listdir(directory)):
        if not name.endswith(".txt"):
            continue
        with open(os.path.join(directory, name), encoding="utf-8") as f:
            text = f.read()
        doc_id = os.path.splitext(name)[0]
        docs.append(Document(doc_id=doc_id, title=doc_id, text=text))
    return docs


def explain():
    return (
        "Loader načte vyčištěné .txt soubory z adresáře do objektů Document "
        "(doc_id, title, text). Je vstupem pro Chunker."
    )
```

- [ ] **Step 4: Spustit test — musí projít**

Run: `python -m pytest tests/test_loader.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add jellyai/loader.py tests/test_loader.py
git commit -m "feat: loader dokumentů"
```

---

### Task 5: Chunker (jellyai/chunker.py)

**Files:**
- Create: `jellyai/chunker.py`
- Test: `tests/test_chunker.py`

**Interfaces:**
- Consumes: `Document` (Task 4), `ChunkerConfig` (Task 1), `split_sentences` (Task 2).
- Produces:
  - dataclass `Passage(doc_id: str, index: int, text: str, start: int, end: int)` (`start`/`end` = indexy vět, `end` exkluzivní).
  - `chunk(document: Document, config: ChunkerConfig) -> list[Passage]`.
  - `explain() -> str`.

- [ ] **Step 1: Napsat failing test**

`tests/test_chunker.py`:
```python
from config import ChunkerConfig
from jellyai.loader import Document
from jellyai.chunker import chunk, Passage, explain


def _doc(n):
    text = " ".join(f"Věta číslo {i}." for i in range(n))
    return Document(doc_id="d", title="d", text=text)


def test_chunk_overlap_and_coverage():
    cfg = ChunkerConfig(size=3, overlap=1)  # step = 2
    passages = chunk(_doc(6), cfg)
    # okna vět: [0:3], [2:5], [4:6]
    assert [(p.start, p.end) for p in passages] == [(0, 3), (2, 5), (4, 6)]
    assert [p.index for p in passages] == [0, 1, 2]
    assert isinstance(passages[0], Passage)
    assert passages[0].doc_id == "d"


def test_chunk_short_document():
    cfg = ChunkerConfig(size=3, overlap=1)
    passages = chunk(_doc(2), cfg)
    assert len(passages) == 1
    assert (passages[0].start, passages[0].end) == (0, 2)


def test_chunk_empty_document():
    cfg = ChunkerConfig(size=3, overlap=1)
    assert chunk(Document("d", "d", "   "), cfg) == []


def test_explain_nonempty():
    assert explain().strip()
```

- [ ] **Step 2: Spustit test — musí selhat**

Run: `python -m pytest tests/test_chunker.py -v`
Expected: FAIL (`ModuleNotFoundError`)

- [ ] **Step 3: Napsat jellyai/chunker.py**

`jellyai/chunker.py`:
```python
from dataclasses import dataclass

from jellyai.text import split_sentences


@dataclass
class Passage:
    doc_id: str
    index: int
    text: str
    start: int   # index první věty
    end: int     # index za poslední větou (exkluzivní)


def chunk(document, config):
    """Rozseká dokument na pasáže po `size` větách s překryvem `overlap`."""
    sentences = split_sentences(document.text)
    n = len(sentences)
    if n == 0:
        return []
    size = max(1, config.size)
    overlap = max(0, min(config.overlap, size - 1))
    step = size - overlap
    passages = []
    idx = 0
    i = 0
    while i < n:
        window = sentences[i:i + size]
        passages.append(Passage(
            doc_id=document.doc_id,
            index=idx,
            text=" ".join(window),
            start=i,
            end=i + len(window),
        ))
        idx += 1
        if i + size >= n:
            break
        i += step
    return passages


def explain():
    return (
        "Chunker rozseká Document na překrývající se pasáže po `size` větách "
        "(sousední pasáže sdílejí `overlap` vět). Překryv zajišťuje, že se odpověď "
        "neztratí na hranici dvou pasáží."
    )
```

- [ ] **Step 4: Spustit test — musí projít**

Run: `python -m pytest tests/test_chunker.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: Commit**

```bash
git add jellyai/chunker.py tests/test_chunker.py
git commit -m "feat: chunker vět s překryvem"
```

---

### Task 6: Retriever — TF-IDF a BM25 od nuly (jellyai/retriever.py)

**Files:**
- Create: `jellyai/retriever.py`
- Test: `tests/test_retriever.py`

**Interfaces:**
- Consumes: `Passage` (Task 5), `RetrieverConfig` (Task 1), `tokenize`/`CZECH_STOPWORDS` (Task 2), numpy.
- Produces:
  - třída `Retriever(config: RetrieverConfig)` s metodami:
    - `build(passages: list[Passage]) -> Retriever` (vrací self).
    - `search(query: str, top_k: int | None = None) -> list[tuple[Passage, float]]` — seřazené sestupně, jen skóre > 0.
  - `explain() -> str`.

- [ ] **Step 1: Napsat failing test**

`tests/test_retriever.py`:
```python
import pytest

from config import RetrieverConfig
from jellyai.chunker import Passage
from jellyai.retriever import Retriever, explain


def _passages():
    return [
        Passage("rur", 0,
                "Roboty vyráběla firma Rossumovy univerzální roboty. "
                "Starý Rossum je vynalezl.", 0, 2),
        Passage("rur", 1,
                "Helena přijela na ostrov. Domin jí ukázal továrnu.", 0, 2),
        Passage("rur", 2,
                "Nána vařila oběd v kuchyni.", 0, 1),
    ]


@pytest.mark.parametrize("method", ["bm25", "tfidf"])
def test_retriever_finds_relevant_passage(method):
    r = Retriever(RetrieverConfig(method=method, top_k=2)).build(_passages())
    results = r.search("roboty")
    assert results, "měl by najít aspoň jednu pasáž"
    assert results[0][0].index == 0  # pasáž o robotech je první


def test_search_no_match_returns_empty():
    r = Retriever(RetrieverConfig(method="bm25")).build(_passages())
    assert r.search("kvantová chromodynamika") == []


def test_search_empty_index():
    r = Retriever(RetrieverConfig()).build([])
    assert r.search("cokoliv") == []


def test_explain_nonempty():
    assert explain().strip()
```

- [ ] **Step 2: Spustit test — musí selhat**

Run: `python -m pytest tests/test_retriever.py -v`
Expected: FAIL (`ModuleNotFoundError`)

- [ ] **Step 3: Napsat jellyai/retriever.py**

`jellyai/retriever.py`:
```python
import math

import numpy as np

from jellyai.text import tokenize, CZECH_STOPWORDS


class Retriever:
    """Vyhledávač pasáží. TF-IDF (kosinová podobnost) nebo BM25, psané od nuly."""

    def __init__(self, config):
        self.config = config
        self.passages = []
        self.vocab = {}          # term -> sloupcový index
        self.counts = None       # matice četností [n_docs, n_terms]
        self.doc_len = None      # délky dokumentů (v tokenech)
        self.avg_len = 0.0
        self.df = None           # document frequency na term
        self.idf = None          # TF-IDF idf
        self._tfidf_norm = None  # normalizované tfidf řádky pro kosinus

    def _tok(self, text):
        sw = CZECH_STOPWORDS if self.config.use_stopwords else None
        return tokenize(text, sw)

    def build(self, passages):
        self.passages = list(passages)
        docs_tokens = [self._tok(p.text) for p in self.passages]
        vocab = {}
        for toks in docs_tokens:
            for t in toks:
                if t not in vocab:
                    vocab[t] = len(vocab)
        self.vocab = vocab
        n_docs = len(self.passages)
        n_terms = len(vocab)
        counts = np.zeros((n_docs, n_terms), dtype=np.float64)
        for i, toks in enumerate(docs_tokens):
            for t in toks:
                counts[i, vocab[t]] += 1.0
        self.counts = counts
        self.doc_len = counts.sum(axis=1)
        self.avg_len = float(self.doc_len.mean()) if n_docs else 0.0
        self.df = (counts > 0).sum(axis=0)
        # TF-IDF idf (smoothed) + normalizované řádky pro kosinovou podobnost
        self.idf = np.log((n_docs + 1) / (self.df + 1)) + 1.0
        tfidf = counts * self.idf
        norms = np.linalg.norm(tfidf, axis=1, keepdims=True)
        norms[norms == 0] = 1.0
        self._tfidf_norm = tfidf / norms
        return self

    def search(self, query, top_k=None):
        if top_k is None:
            top_k = self.config.top_k
        if not self.passages:
            return []
        tokens = self._tok(query)
        if self.config.method == "tfidf":
            scores = self._tfidf_scores(tokens)
        else:
            scores = self._bm25_scores(tokens)
        order = np.argsort(-scores)[:top_k]
        return [(self.passages[i], float(scores[i])) for i in order if scores[i] > 0]

    def _tfidf_scores(self, tokens):
        v = np.zeros(len(self.vocab))
        for t in tokens:
            j = self.vocab.get(t)
            if j is not None:
                v[j] += 1.0
        v *= self.idf
        n = np.linalg.norm(v)
        if n > 0:
            v /= n
        return self._tfidf_norm @ v

    def _bm25_scores(self, tokens):
        k1 = self.config.k1
        b = self.config.b
        n_docs = len(self.passages)
        scores = np.zeros(n_docs)
        if self.avg_len == 0:
            return scores
        for t in set(tokens):
            j = self.vocab.get(t)
            if j is None:
                continue
            f = self.counts[:, j]
            idf = math.log((n_docs - self.df[j] + 0.5) / (self.df[j] + 0.5) + 1.0)
            denom = f + k1 * (1 - b + b * self.doc_len / self.avg_len)
            scores += idf * (f * (k1 + 1)) / denom
        return scores


def explain():
    return (
        "Retriever indexuje pasáže a k dotazu vrátí ty nejrelevantnější.\n"
        "TF-IDF: každé slovo váží podle vzácnosti (idf), dokumenty i dotaz jsou "
        "vektory, podobnost = kosinus úhlu mezi nimi.\n"
        "BM25: vylepšené skórování s nasycením četnosti (k1) a normalizací délky "
        "dokumentu (b) — obvykle lepší než čisté TF-IDF.\n"
        "Vše počítáno od nuly v numpy, žádný stažený model."
    )
```

- [ ] **Step 4: Spustit test — musí projít**

Run: `python -m pytest tests/test_retriever.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: Commit**

```bash
git add jellyai/retriever.py tests/test_retriever.py
git commit -m "feat: retriever TF-IDF a BM25 od nuly"
```

---

### Task 7: Answerer — rozhraní + extraktivní (jellyai/answerer/)

**Files:**
- Create: `jellyai/answerer/base.py`
- Create: `jellyai/answerer/extractive.py`
- Test: `tests/test_answerer.py`

**Interfaces:**
- Consumes: `Passage` (Task 5), `AnswererConfig` (Task 1), `tokenize`/`split_sentences`/`CZECH_STOPWORDS` (Task 2).
- Produces:
  - `base.py`: dataclass `Answer(text: str, sources: list[str], score: float)`; abstraktní `Answerer` s metodou `answer(question, retrieved) -> Answer`.
  - `extractive.py`: `ExtractiveAnswerer(config: AnswererConfig)` s `answer(question: str, retrieved: list[tuple[Passage, float]]) -> Answer`; `explain() -> str`.
- Formát zdroje: `"{doc_id}#{passage_index}"`.
- Fallback text při žádné shodě: `"V textu jsem odpověď nenašel."`

- [ ] **Step 1: Napsat failing test**

`tests/test_answerer.py`:
```python
from config import AnswererConfig
from jellyai.chunker import Passage
from jellyai.answerer.base import Answer, Answerer
from jellyai.answerer.extractive import ExtractiveAnswerer, explain


def _retrieved():
    p = Passage("rur", 0,
                "Roboty vyráběla firma Rossumovy univerzální roboty. "
                "Starý Rossum je vynalezl.", 0, 2)
    return [(p, 1.0)]


def test_extractive_picks_relevant_sentence():
    a = ExtractiveAnswerer(AnswererConfig(template=False))
    ans = a.answer("kdo vyráběl roboty", _retrieved())
    assert isinstance(ans, Answer)
    assert "roboty" in ans.text.lower()
    assert ans.sources == ["rur#0"]
    assert ans.score > 0


def test_extractive_template_prefix():
    a = ExtractiveAnswerer(AnswererConfig(template=True))
    ans = a.answer("kdo vyráběl roboty", _retrieved())
    assert ans.text.startswith("Podle textu:")


def test_extractive_no_results():
    a = ExtractiveAnswerer(AnswererConfig())
    ans = a.answer("cokoliv", [])
    assert ans.sources == []
    assert "nenašel" in ans.text.lower()


def test_base_is_abstract():
    try:
        Answerer().answer("q", [])
        assert False, "mělo vyhodit NotImplementedError"
    except NotImplementedError:
        pass


def test_explain_nonempty():
    assert explain().strip()
```

- [ ] **Step 2: Spustit test — musí selhat**

Run: `python -m pytest tests/test_answerer.py -v`
Expected: FAIL (`ModuleNotFoundError`)

- [ ] **Step 3: Napsat base.py a extractive.py**

`jellyai/answerer/base.py`:
```python
from dataclasses import dataclass, field


@dataclass
class Answer:
    text: str
    sources: list = field(default_factory=list)
    score: float = 0.0


class Answerer:
    """Rozhraní bloku Answerer. Podtřídy implementují answer()."""

    def answer(self, question, retrieved):
        raise NotImplementedError
```

`jellyai/answerer/extractive.py`:
```python
from jellyai.answerer.base import Answer, Answerer
from jellyai.text import tokenize, split_sentences, CZECH_STOPWORDS

_NO_ANSWER = "V textu jsem odpověď nenašel."


class ExtractiveAnswerer(Answerer):
    """Vybere z nalezených pasáží nejrelevantnější větu (extraktivní odpověď)."""

    def __init__(self, config):
        self.config = config

    def answer(self, question, retrieved):
        if not retrieved:
            return Answer(text=_NO_ANSWER, sources=[], score=0.0)
        q_tokens = set(tokenize(question, CZECH_STOPWORDS))
        best = None  # (score, sentence, source)
        for passage, _pscore in retrieved:
            for sent in split_sentences(passage.text):
                s_tokens = tokenize(sent, CZECH_STOPWORDS)
                if not s_tokens:
                    continue
                overlap = sum(1 for t in s_tokens if t in q_tokens)
                if overlap == 0:
                    continue
                score = overlap / (len(s_tokens) ** 0.5)
                source = f"{passage.doc_id}#{passage.index}"
                if best is None or score > best[0]:
                    best = (score, sent, source)
        if best is None:
            return Answer(text=_NO_ANSWER, sources=[], score=0.0)
        score, sentence, source = best
        text = f"Podle textu: {sentence}" if self.config.template else sentence
        return Answer(text=text, sources=[source], score=float(score))


def explain():
    return (
        "ExtractiveAnswerer vezme pasáže od Retrieveru, rozdělí je na věty a vybere "
        "tu s největším překryvem slov s dotazem (normalizováno délkou věty). "
        "Vrací větu doslova z textu + zdroj (doc_id#index), takže odpověď je "
        "dohledatelná a nevymýšlí si. Volitelná šablona přidá prefix 'Podle textu:'."
    )
```

- [ ] **Step 4: Spustit test — musí projít**

Run: `python -m pytest tests/test_answerer.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: Commit**

```bash
git add jellyai/answerer/base.py jellyai/answerer/extractive.py tests/test_answerer.py
git commit -m "feat: extraktivní answerer + rozhraní"
```

---

### Task 8: Pipeline (jellyai/pipeline.py)

**Files:**
- Create: `jellyai/pipeline.py`
- Test: `tests/test_pipeline.py`

**Interfaces:**
- Consumes: `load_documents` (Task 4), `chunk` (Task 5), `Retriever` (Task 6), `ExtractiveAnswerer` (Task 7), `Config` (Task 1), `Answer` (Task 7).
- Produces:
  - `QAPipeline(retriever, answerer)` s `ask(question: str) -> Answer`.
  - classmethod `QAPipeline.from_corpus(directory: str, config: Config) -> QAPipeline`.
  - `explain() -> str`.

- [ ] **Step 1: Napsat failing test**

`tests/test_pipeline.py`:
```python
from config import Config
from jellyai.pipeline import QAPipeline, explain


def _corpus(tmp_path):
    (tmp_path / "rur.txt").write_text(
        "Roboty vyráběla firma Rossumovy univerzální roboty. "
        "Starý Rossum je vynalezl. "
        "Helena přijela na ostrov. "
        "Domin jí ukázal továrnu.",
        encoding="utf-8",
    )
    return str(tmp_path)


def test_pipeline_end_to_end(tmp_path):
    pipe = QAPipeline.from_corpus(_corpus(tmp_path), Config())
    ans = pipe.ask("kdo vyráběl roboty")
    assert "roboty" in ans.text.lower()
    assert ans.sources and ans.sources[0].startswith("rur#")


def test_pipeline_no_answer(tmp_path):
    pipe = QAPipeline.from_corpus(_corpus(tmp_path), Config())
    ans = pipe.ask("kvantová chromodynamika")
    assert "nenašel" in ans.text.lower()


def test_explain_nonempty():
    assert explain().strip()
```

- [ ] **Step 2: Spustit test — musí selhat**

Run: `python -m pytest tests/test_pipeline.py -v`
Expected: FAIL (`ModuleNotFoundError`)

- [ ] **Step 3: Napsat jellyai/pipeline.py**

`jellyai/pipeline.py`:
```python
from jellyai.loader import load_documents
from jellyai.chunker import chunk
from jellyai.retriever import Retriever
from jellyai.answerer.extractive import ExtractiveAnswerer


class QAPipeline:
    """Pospojuje bloky: otázka → retrieval → extraktivní odpověď."""

    def __init__(self, retriever, answerer):
        self.retriever = retriever
        self.answerer = answerer

    def ask(self, question):
        retrieved = self.retriever.search(question)
        return self.answerer.answer(question, retrieved)

    @classmethod
    def from_corpus(cls, directory, config):
        docs = load_documents(directory)
        passages = []
        for doc in docs:
            passages.extend(chunk(doc, config.chunker))
        retriever = Retriever(config.retriever).build(passages)
        answerer = ExtractiveAnswerer(config.answerer)
        return cls(retriever, answerer)


def explain():
    return (
        "QAPipeline spojí Retriever a Answerer do jednoho celku: ask(otázka) "
        "vyhledá relevantní pasáže a nechá Answerer složit odpověď. "
        "from_corpus() postaví celou pipeline z adresáře s vyčištěnými texty."
    )
```

- [ ] **Step 4: Spustit test — musí projít**

Run: `python -m pytest tests/test_pipeline.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add jellyai/pipeline.py tests/test_pipeline.py
git commit -m "feat: QA pipeline (otázka → odpověď)"
```

---

### Task 9: Výuková vrstva explain (jellyai/explain.py)

**Files:**
- Create: `jellyai/explain.py`
- Test: `tests/test_explain.py`

**Interfaces:**
- Consumes: modulové `explain()` z loader/chunker/retriever/pipeline (Tasks 4–8) a `answerer.extractive` (Task 7).
- Produces:
  - `explain_block(name: str) -> str` — pro `name ∈ {loader, chunker, retriever, answerer, pipeline}`; jinak `KeyError`.
  - `list_blocks() -> list[str]` — seřazený seznam názvů.

- [ ] **Step 1: Napsat failing test**

`tests/test_explain.py`:
```python
import pytest

from jellyai.explain import explain_block, list_blocks


def test_list_blocks():
    assert list_blocks() == ["answerer", "chunker", "loader", "pipeline", "retriever"]


def test_explain_block_returns_text():
    for name in list_blocks():
        text = explain_block(name)
        assert isinstance(text, str) and text.strip()


def test_explain_unknown_block():
    with pytest.raises(KeyError):
        explain_block("neexistuje")
```

- [ ] **Step 2: Spustit test — musí selhat**

Run: `python -m pytest tests/test_explain.py -v`
Expected: FAIL (`ModuleNotFoundError`)

- [ ] **Step 3: Napsat jellyai/explain.py**

`jellyai/explain.py`:
```python
from jellyai import loader, chunker, retriever, pipeline
from jellyai.answerer import extractive

_BLOCKS = {
    "loader": loader.explain,
    "chunker": chunker.explain,
    "retriever": retriever.explain,
    "answerer": extractive.explain,
    "pipeline": pipeline.explain,
}


def explain_block(name):
    if name not in _BLOCKS:
        raise KeyError(
            f"Neznámý blok: {name}. Dostupné: {', '.join(sorted(_BLOCKS))}"
        )
    return _BLOCKS[name]()


def list_blocks():
    return sorted(_BLOCKS)
```

- [ ] **Step 4: Spustit test — musí projít**

Run: `python -m pytest tests/test_explain.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add jellyai/explain.py tests/test_explain.py
git commit -m "feat: výuková vrstva explain pro bloky"
```

---

### Task 10: Download public-domain knih (dataprep/download.py)

**Files:**
- Create: `dataprep/download.py`
- Test: `tests/test_download.py`

**Interfaces:**
- Consumes: `Config`/`DataConfig` (Task 1), stdlib `urllib`.
- Produces:
  - `book_targets(config: Config) -> list[tuple[str, str]]` — dvojice `(url, dest_path)` z `config.data.books`.
  - `download_books(config: Config) -> list[tuple[str, bool]]` — stáhne do `raw_dir`, vrátí `(dest, success)`; chyby přeskočí s hláškou.

- [ ] **Step 1: Napsat failing test**

`tests/test_download.py`:
```python
import os

from config import Config, DataConfig
from dataprep.download import book_targets


def test_book_targets_builds_paths():
    cfg = Config(data=DataConfig(
        raw_dir="data/raw",
        books=[("https://example.com/rur.txt", "rur.txt")],
    ))
    targets = book_targets(cfg)
    assert targets == [("https://example.com/rur.txt",
                        os.path.join("data/raw", "rur.txt"))]


def test_book_targets_empty():
    assert book_targets(Config()) == []
```

- [ ] **Step 2: Spustit test — musí selhat**

Run: `python -m pytest tests/test_download.py -v`
Expected: FAIL (`ModuleNotFoundError`)

- [ ] **Step 3: Napsat dataprep/download.py**

`dataprep/download.py`:
```python
import os
import urllib.request


def book_targets(config):
    """Z konfigurace vytvoří dvojice (url, cílová cesta v raw_dir)."""
    targets = []
    for url, filename in config.data.books:
        targets.append((url, os.path.join(config.data.raw_dir, filename)))
    return targets


def download_books(config):
    """Stáhne knihy do raw_dir. Chyby přeskočí a nahlásí. Vrátí (cesta, úspěch)."""
    os.makedirs(config.data.raw_dir, exist_ok=True)
    results = []
    for url, dest in book_targets(config):
        try:
            urllib.request.urlretrieve(url, dest)
            print(f"Staženo: {dest}")
            results.append((dest, True))
        except Exception as exc:  # noqa: BLE001 - chceme pokračovat dalšími
            print(f"Přeskočeno {url}: {exc}")
            results.append((dest, False))
    return results
```

- [ ] **Step 4: Spustit test — musí projít**

Run: `python -m pytest tests/test_download.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add dataprep/download.py tests/test_download.py
git commit -m "feat: download public-domain knih (stdlib urllib)"
```

---

### Task 11: CLI (cli.py) + seed R.U.R. korpusu

**Files:**
- Create: `cli.py`
- Modify: `config.py` (naplnit `DataConfig.books` a přidat helper pro výchozí config)
- Test: `tests/test_cli.py`

**Interfaces:**
- Consumes: `Config` (Task 1), `download_books` (Task 10), `build_processed` (Task 3), `QAPipeline` (Task 8), `explain_block`/`list_blocks` (Task 9), `Document` (Task 4).
- Produces:
  - `cli.py` s funkcemi: `cmd_prepare_data(config)`, `cmd_build_index(config)`, `cmd_ask(config, question) -> str`, `cmd_explain(name) -> str`, `main(argv=None)`.
  - `cmd_ask` vrací formátovaný string `"<odpověď>\n(zdroj: <sources>)"`.
- **Seed:** `prepare-data` po stažení zkopíruje i existující `training_text/karel_capek_rur.txt` do `raw_dir` (aby QA fungovalo i bez sítě), pak vyčistí celý `raw_dir` do `processed_dir`.

- [ ] **Step 1: Doplnit výchozí seznam knih do config.py**

V `config.py` uprav default `DataConfig.books` na kurátorovaný seznam (URL ověř při běhu; nedostupné `prepare-data` přeskočí). Nahraď pole `books` tímto default:
```python
@dataclass
class DataConfig:
    raw_dir: str = "data/raw"
    processed_dir: str = "data/processed"
    # (url, filename) — public-domain české texty; ověřeno při stažení,
    # nedostupné se přeskočí. R.U.R. se navíc seeduje z training_text/.
    books: list = field(default_factory=lambda: [
        # Project Gutenberg / Wikizdroje — doplní/ověří se při realizaci Tasku 11.
    ])
```

- [ ] **Step 2: Napsat failing test CLI**

`tests/test_cli.py`:
```python
from config import Config, DataConfig
from jellyai.pipeline import QAPipeline
import cli


def _prepared(tmp_path):
    proc = tmp_path / "processed"
    proc.mkdir()
    (proc / "rur.txt").write_text(
        "Roboty vyráběla firma Rossumovy univerzální roboty. "
        "Starý Rossum je vynalezl.",
        encoding="utf-8",
    )
    return Config(data=DataConfig(processed_dir=str(proc)))


def test_cmd_ask_formats_answer(tmp_path):
    cfg = _prepared(tmp_path)
    out = cli.cmd_ask(cfg, "kdo vyráběl roboty")
    assert "roboty" in out.lower()
    assert "zdroj" in out.lower()


def test_cmd_explain_known():
    out = cli.cmd_explain("retriever")
    assert "BM25" in out or "TF-IDF" in out


def test_main_ask_smoke(tmp_path, capsys):
    cfg = _prepared(tmp_path)
    # main používá processed_dir z argumentu --processed-dir
    cli.main(["ask", "kdo vyráběl roboty", "--processed-dir", cfg.data.processed_dir])
    captured = capsys.readouterr()
    assert "roboty" in captured.out.lower()
```

- [ ] **Step 3: Spustit test — musí selhat**

Run: `python -m pytest tests/test_cli.py -v`
Expected: FAIL (`ModuleNotFoundError: No module named 'cli'`)

- [ ] **Step 4: Napsat cli.py**

`cli.py`:
```python
import argparse
import os
import shutil

from config import Config, DataConfig
from dataprep.download import download_books
from dataprep.clean import build_processed
from jellyai.pipeline import QAPipeline
from jellyai.explain import explain_block, list_blocks

_SEED_RUR = "training_text/karel_capek_rur.txt"


def cmd_prepare_data(config):
    os.makedirs(config.data.raw_dir, exist_ok=True)
    download_books(config)
    # seed R.U.R. z původního repa, aby QA fungovalo i bez sítě
    if os.path.exists(_SEED_RUR):
        shutil.copyfile(_SEED_RUR, os.path.join(config.data.raw_dir, "rur.txt"))
    written = build_processed(config.data.raw_dir, config.data.processed_dir)
    print(f"Připraveno {len(written)} textů v {config.data.processed_dir}")
    return written


def cmd_build_index(config):
    pipe = QAPipeline.from_corpus(config.data.processed_dir, config)
    n = len(pipe.retriever.passages)
    print(f"Index postaven: {n} pasáží z {config.data.processed_dir}")
    return n


def cmd_ask(config, question):
    pipe = QAPipeline.from_corpus(config.data.processed_dir, config)
    ans = pipe.ask(question)
    src = ", ".join(ans.sources) if ans.sources else "—"
    return f"{ans.text}\n(zdroj: {src})"


def cmd_explain(name):
    return explain_block(name)


def main(argv=None):
    parser = argparse.ArgumentParser(prog="cli", description="Český QA nad texty (V1)")
    parser.add_argument("--processed-dir", default=None,
                        help="adresář s vyčištěnými texty")
    sub = parser.add_subparsers(dest="command", required=True)
    sub.add_parser("prepare-data", help="stáhne + vyčistí texty")
    sub.add_parser("build-index", help="postaví index a vypíše statistiku")
    p_ask = sub.add_parser("ask", help="odpoví na dotaz")
    p_ask.add_argument("question", help="otázka v češtině")
    p_explain = sub.add_parser("explain", help="popíše blok")
    p_explain.add_argument("block", nargs="?", help=f"jeden z: {', '.join(list_blocks())}")

    args = parser.parse_args(argv)
    config = Config()
    if args.processed_dir:
        config = Config(data=DataConfig(processed_dir=args.processed_dir))

    if args.command == "prepare-data":
        cmd_prepare_data(config)
    elif args.command == "build-index":
        cmd_build_index(config)
    elif args.command == "ask":
        print(cmd_ask(config, args.question))
    elif args.command == "explain":
        if not args.block:
            print("Dostupné bloky: " + ", ".join(list_blocks()))
        else:
            print(cmd_explain(args.block))


if __name__ == "__main__":
    main()
```

- [ ] **Step 5: Spustit test — musí projít**

Run: `python -m pytest tests/test_cli.py -v`
Expected: PASS (3 passed)

- [ ] **Step 6: Commit**

```bash
git add cli.py config.py tests/test_cli.py
git commit -m "feat: CLI (prepare-data/build-index/ask/explain) + seed R.U.R."
```

---

### Task 12: Úklid staré LSTM verze, README, ověření end-to-end

**Files:**
- Delete: `dataset.py`, `model.py`, `main.py`, `train.py`, `train_sentencepiece.py`, `helpers.py`, `settings.py`
- Delete: `saved_weights/best_model.pth`
- Keep: `training_text/karel_capek_rur.txt` (seed korpusu)
- Modify/Create: `README.md`

**Interfaces:**
- Consumes: hotová V1 pipeline a CLI.
- Produces: čistý repo bez starého LSTM kódu (zůstává v git historii) + README s postupem.

- [ ] **Step 1: Odstranit starý LSTM kód**

```bash
git rm dataset.py model.py main.py train.py train_sentencepiece.py helpers.py settings.py saved_weights/best_model.pth
```
(LSTM verze zůstává dohledatelná v git historii — commit `2a4cdc8` a starší.)

- [ ] **Step 2: Napsat README.md**

`README.md`:
```markdown
# jellyAI3 — český QA nad texty (V1)

Výuková knihovna skládatelných bloků pro odpovídání na dotazy o obsahu českých
textů. Fakta se berou přímo z textu přes retrieval (ne z „paměti" modelu).
Plně lokální, bez externích služeb.

## Setup

Vyžaduje Python 3.11 nebo 3.12 (novější verze mají problém s torch, který přijde ve V2).

```bash
python3.12 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Použití

```bash
# 1) připravit data (stáhne knihy + naseeduje R.U.R., vyčistí do data/processed)
python cli.py prepare-data

# 2) zeptat se
python cli.py ask "kdo vyráběl roboty?"
python cli.py ask "co znamená R.U.R.?"

# statistika indexu
python cli.py build-index

# jak funguje který blok
python cli.py explain retriever
```

## Bloky

Loader → Chunker → Retriever (TF-IDF/BM25 od nuly) → ExtractiveAnswerer → Pipeline.
Konfigurace v `config.py`. Každý blok umí `explain()`.

## Testy

```bash
python -m pytest -v
```

## Roadmapa

- **V2:** syntetické QA páry + malý seq2seq generátor od nuly (plynulejší odpovědi).
- Detaily v `docs/superpowers/specs/2026-07-15-cesky-gpt-design.md`.
```

- [ ] **Step 3: Spustit celou testovou sadu**

Run: `python -m pytest -v`
Expected: PASS (všechny testy Tasků 1–11)

- [ ] **Step 4: Ověřit end-to-end ručně**

```bash
python cli.py prepare-data
python cli.py ask "kdo vyráběl roboty?"
```
Expected: odpověď obsahuje větu z R.U.R. o robotech + řádek `(zdroj: rur#...)`.
Vyzkoušej i `python cli.py ask "co znamená R.U.R.?"` a `python cli.py explain retriever`.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: odstranění staré LSTM verze, README, V1 hotová"
```

---

## Self-Review (kontrola plánu proti specu)

**Spec coverage:** Loader (T4), Chunker (T5), Retriever TF-IDF/BM25 od nuly (T6), extraktivní Answerer + rozhraní (T7), Pipeline (T8), explain vrstva (T9), data download + clean (T10, T3), CLI prepare-data/build-index/ask/explain (T11), zachování češtiny (T3), testy hermetické (všude tmp_path), config bloků (T1). Ošetření chyb: prázdný processed dir (T4 FileNotFoundError), žádná shoda → „nenašel jsem" (T7/T8), prázdný index (T6), download selže → přeskočí (T10). ✔ Pokryto.

**Odchylky od specu (záměrné, ku prospěchu):** (1) datové skripty jsou v `dataprep/` místo `data/` — `data/` zůstává čistě úložiště (raw/processed), aby se package nemíchal s daty. (2) Download používá stdlib `urllib` místo `requests` → o závislost méně. Obojí v souladu s cílem „minimum závislostí".

**Placeholder scan:** žádné TBD/„handle edge cases"/„similar to Task N"; každý krok má konkrétní kód a příkaz. Jediné vědomé „doplní se při běhu" je konkrétní seznam URL knih v `DataConfig.books` — to je ověření dostupnosti živých zdrojů, ne chybějící logika (fungování QA na tom nestojí díky seedu R.U.R.).

**Type consistency:** `Document`, `Passage`, `Answer`, `Config` a jejich pole jsou používány konzistentně napříč Tasky; `Retriever.search` vrací `list[(Passage, float)]`, což `ExtractiveAnswerer.answer` i `QAPipeline.ask` konzumují ve stejném tvaru; formát zdroje `"{doc_id}#{index}"` je stejný v T7 i testech T8/T11. ✔
